import { Download, ExternalLink, X } from 'lucide-react';

export function PdfViewerDialog({ file, onClose, onDownload }: { file: { id: string; name: string; url: string } | null; onClose: () => void; onDownload: (file: { id: string; name: string }) => Promise<void> }) {
  if (!file) return null;
  return <div className="fixed inset-0 z-40 flex flex-col bg-slate-950/65 p-3 sm:p-6" role="dialog" aria-modal="true" aria-label={`Preview ${file.name}`}><section className="mx-auto flex h-full w-full max-w-6xl flex-col overflow-hidden rounded-xl bg-white shadow-2xl"><header className="flex items-center justify-between gap-4 border-b px-4 py-3"><h2 className="min-w-0 truncate text-sm font-semibold" title={file.name}>{file.name}</h2><div className="flex shrink-0 items-center gap-1"><button type="button" onClick={() => void onDownload(file)} title="Download PDF" aria-label="Download PDF" className="rounded p-2 text-slate-500 hover:bg-slate-100 hover:text-brand"><Download size={18} /></button><a href={file.url} target="_blank" rel="noreferrer" title="Open in a new tab" className="rounded p-2 text-slate-500 hover:bg-slate-100 hover:text-brand"><ExternalLink size={18} /></a><button type="button" onClick={onClose} title="Close preview" aria-label="Close preview" className="rounded p-2 text-slate-500 hover:bg-slate-100 hover:text-slate-700"><X size={19} /></button></div></header><iframe title={`PDF preview: ${file.name}`} src={file.url} className="min-h-0 flex-1 bg-slate-100" /></section></div>;
}
